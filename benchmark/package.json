{
  "name": "@onchain-access-control/benchmark",
  "version": "0.0.1",
  "author": {
    "name": "Fabian Piper",
    "url": "https://github.com/fapiper"
  },
  "scripts": {
    "bench-run": "npx caliper launch manager --caliper-bind-sut ethereum:latest --caliper-workspace . --caliper-networkconfig network/env-ethereum.json --caliper-benchconfig scenario/oac/config.yaml --caliper-report-path reports/report.html --caliper-flow-only-test"
  },
  "dependencies": {
    "@hyperledger/caliper-cli": "0.5.0",
    "@hyperledger/caliper-core": "^0.5.0",
    "@hyperledger/caliper-ethereum": "^0.5.0",
    "web3": "^1.3.0"
  }
}
